Screen {
    align: center middle;
}

Horizontal {
    width: 80%;
    height: 80%;
}

DataTable {
    padding: 1;
    border: solid $accent;
    width: 1fr;
}

Pretty {
    width: 1fr;
    border: solid $accent;
}